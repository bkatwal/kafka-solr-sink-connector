language: java
jdk:
  - openjdk8

sudo: false # faster builds

notification:
  email: bikas.katwal10@gmail.com

# This is to enable CodeCov's coverage
# If a build is successful, the code is submitted for coverage analysis
after_success:
  - bash <(curl -s https://codecov.io/bash)